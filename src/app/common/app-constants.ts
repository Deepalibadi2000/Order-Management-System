import { NgbModalOptions } from "@ng-bootstrap/ng-bootstrap";

export class AppConstants {

    public static NGB_MODAL_OPTIONS: NgbModalOptions = {
        scrollable: true,
        size: 'lg',
        centered: true,
        keyboard: false,
        backdrop: 'static',
    }
}